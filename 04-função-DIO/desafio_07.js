/* Desafio para verificar se uma pessoa é maior de idade ou não. */
function verificarIdade(idade) {
    if (idade < 18) { return 'menor de idade' } else {return 'maior de idade'}};

    console.log(verificarIdade(26));